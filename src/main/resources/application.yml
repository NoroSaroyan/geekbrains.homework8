spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hibernatedb?currentSchema=onlineShop
    username: testOnlineShop1
    password: testpswrd1
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    show-sql: true
    hibernate:
      ddl-auto: none
  thymeleaf:
    check-template-location: true
    servlet:
      content-type: text/html
    enabled: true
    encoding: UTF-8
    mode: HTML5
    suffix: .html
    prefix: classpath:/templates/